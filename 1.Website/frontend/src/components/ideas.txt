1. Maybe a seperate component that handles the display of graphs :
 1.1 allow user to add task
 1.2 allow user to remove task
 1.3 allow user to add user 
 1.4 allow user to remove user

2. Another component that shows users users working on the graph 

3. Component that will show the progress of the graph
